.media
  .media-left
    = link_to user_path(comment.user) do
      = image_tag(comment.user.avatar, class: "media-object", alt: "64x64", height: "64px", width: "64px")
  .media-body
    %h4#top-aligned-media.media-heading
      = comment.user.username
    %p= comment.content
